import React from 'react';

import Heading from '../common/Heading/Heading';

const Feed = () => {
    return (
        <section className="section">
            <Heading type={1}>Feed</Heading>
        </section>
    );
};

export default Feed;